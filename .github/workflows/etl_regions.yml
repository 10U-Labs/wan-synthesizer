---
concurrency:
  cancel-in-progress: false
  group: etl-regions
env:
  AWS_USE_FIPS_ENDPOINT: "true"
jobs:
  assert-no-comments:
    name: assert-no-comments
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: pip install assert-no-comments
      - name: Assert nothing this repository publishes carries a comment
        run: >-
          assert-no-comments
          --annotate
          --exclude 'src/www/spa/vendor/*'
          --verbose
          .github/workflows
          lib
          src
          test
  assert-no-inline-directives:
    name: assert-no-inline-directives
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: pip install assert-no-inline-directives
      - name: Assert no inline directives
        run: >-
          assert-no-inline-directives
          --tools pylint,mypy,yamllint
          --verbose
          ${{ github.workspace }}/src/etl/regions
          ${{ github.workspace }}/test/etl/regions
  assert-no-pytest-plugin-declarations:
    name: assert-no-pytest-plugin-declarations
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: pip install assert-no-pytest-plugin-declarations
      - name: Assert no pytest plugin declarations
        run: >-
          assert-no-pytest-plugin-declarations
          --verbose
          ${{ github.workspace }}/test/etl/regions
  assert-one-assert-per-pytest:
    name: assert-one-assert-per-pytest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: pip install assert-one-assert-per-pytest
      - name: Assert one assert per pytest
        run: >-
          assert-one-assert-per-pytest
          --verbose
          ${{ github.workspace }}/test/etl/regions
  assert-pytest-class-holds-state:
    name: assert-pytest-class-holds-state
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: pip install assert-pytest-class-holds-state
      - name: Assert every pytest class holds state
        run: >-
          assert-pytest-class-holds-state
          --verbose
          ${{ github.workspace }}/test/etl/regions
  assert-pytest-fixture-name-is-needed:
    name: assert-pytest-fixture-name-is-needed
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: pip install assert-pytest-fixture-name-is-needed
      - name: Assert every pytest fixture name is needed
        run: >-
          assert-pytest-fixture-name-is-needed
          --verbose
          ${{ github.workspace }}/test/etl/regions
  assert-pytest-test-can-fail:
    name: assert-pytest-test-can-fail
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: pip install assert-pytest-test-can-fail
      - name: Assert every pytest test can fail
        run: >-
          assert-pytest-test-can-fail
          --verbose
          ${{ github.workspace }}/test/etl/regions
  copy-paste-source:
    name: copy-paste-source
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: npm install -g jscpd
      - name: Detect copy-paste in source
        run: >-
          jscpd --pattern "**/*.py" --threshold 0
          --reporters console src/etl/regions/
  copy-paste-tests:
    name: copy-paste-tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: npm install -g jscpd
      - name: Detect copy-paste in tests
        run: >-
          jscpd --pattern "**/*.py" --threshold 0
          --reporters console test/etl/regions/
  load:
    if: github.ref == 'refs/heads/main'
    name: load
    needs:
      - assert-no-comments
      - assert-no-inline-directives
      - assert-no-pytest-plugin-declarations
      - assert-one-assert-per-pytest
      - assert-pytest-class-holds-state
      - assert-pytest-fixture-name-is-needed
      - assert-pytest-test-can-fail
      - copy-paste-source
      - copy-paste-tests
      - mypy-source
      - mypy-tests
      - pre-deployment-tests
      - pylint-source
      - pylint-tests
      - yamllint
    permissions:
      actions: read
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v5
        with:
          aws-region: us-east-2
          role-to-assume: ${{ vars.OIDC_ROLE_ARN }}
      - env:
          ALL: ${{ inputs.all }}
          BEFORE: ${{ github.event.before }}
          GH_TOKEN: ${{ github.token }}
        name: Load the regions when their file changed since the last load
        run: |
          RUNS="repos/$GITHUB_REPOSITORY/actions/workflows/etl_regions.yml/runs"
          SINCE=$(gh api "$RUNS?branch=main&status=success&per_page=1" \
            --jq '.workflow_runs[0].head_sha // empty')
          if [ -z "$SINCE" ]; then
            SINCE=$BEFORE
          fi
          if [ "$ALL" = "true" ]; then
            SINCE=""
            echo "Loading the regions, changed or not."
          else
            echo "Loading the regions if their file changed since $SINCE."
          fi
          KEY=$(aws ssm get-parameter --name /api.10ulabs.com/api-key \
            --with-decryption --query Parameter.Value --output text)
          echo "::add-mask::$KEY"
          API_KEY="$KEY" PYTHONPATH=src:lib/python python3 -c \
            'import sys; from etl.regions import load_regions; sys.exit(load_regions.main(sys.argv[1:]))' \
            --since "$SINCE"
  mypy-source:
    name: mypy-source
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: pip install mypy
      - name: Run mypy on source
        run: >-
          MYPYPATH=lib/python:src
          python3 -m mypy --strict --explicit-package-bases
          --ignore-missing-imports
          src/etl/regions/
  mypy-tests:
    name: mypy-tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: pip install mypy pytest pyyaml types-PyYAML
      - name: Run mypy on tests
        run: >-
          MYPYPATH=lib/python:src
          python3 -m mypy --strict --explicit-package-bases
          --ignore-missing-imports
          test/etl/regions/
  post-deployment-e2e-tests:
    if: github.ref == 'refs/heads/main'
    name: post-deployment-e2e-tests
    needs:
      - load
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v5
        with:
          aws-region: us-east-2
          role-to-assume: ${{ vars.OIDC_ROLE_ARN }}
      - name: Install tools
        run: pip install pytest
      - name: Read the regions back and hold them to their file
        run: |
          KEY=$(aws ssm get-parameter --name /api.10ulabs.com/api-key \
            --with-decryption --query Parameter.Value --output text)
          echo "::add-mask::$KEY"
          API_KEY="$KEY" PYTHONPATH=.:lib/python:src python3 -m pytest \
            test/etl/regions/post_deployment/e2e/ \
            --import-mode=importlib --confcutdir=test \
            --verbose --pythonwarnings=error
  pre-deployment-tests:
    name: pre-deployment-tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: pip install pytest pytest-cov pyyaml
      - name: Run the unit and integration tiers with a full coverage gate
        run: >-
          PYTHONPATH=.:lib/python:src
          python3 -m pytest
          test/etl/regions/pre_deployment/
          --import-mode=importlib --confcutdir=test
          --verbose --pythonwarnings=error
          --cov=src/etl/regions
          --cov-branch
          --cov-report=term-missing
          --cov-fail-under=100
  pylint-source:
    name: pylint-source
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: pip install pylint
      - name: Run pylint on source
        run: >-
          PYTHONPATH=lib/python:src
          python3 -m pylint --recursive=y
          src/etl/regions/
          --disable=missing-module-docstring,missing-class-docstring,missing-function-docstring
          --fail-on=C,R,W --fail-under=10.0
  pylint-tests:
    name: pylint-tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: pip install pylint pytest pyyaml
      - name: Run pylint on tests
        run: >-
          PYTHONPATH=.:lib/python:src
          python3 -m pylint --recursive=y
          test/etl/regions/
          --disable=missing-module-docstring,missing-class-docstring,missing-function-docstring
          --fail-on=C,R,W --fail-under=10.0
  yamllint:
    name: yamllint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: pip install assert-no-inline-directives yamllint
      - name: Assert no inline yamllint disables
        run: >-
          assert-no-inline-directives
          --tools yamllint
          --verbose
          .github/workflows/etl_regions.yml
      - name: Lint YAML
        run: |
          EMPTY='empty-lines: {max: 0, max-start: 0, max-end: 1}'
          KEY='key-ordering: enable'
          LL='line-length: disable'
          NL='new-line-at-end-of-file: enable'
          TR='truthy: {allowed-values: ["true", "false", "on"]}'
          RULES="$EMPTY, $KEY, $LL, $NL, $TR"
          yamllint --strict \
            --config-data \
            "{extends: default, rules: {$RULES}}" \
            .github/workflows/etl_regions.yml
name: etl_regions
'on':
  push:
    branches: [main]
    paths:
      - data/providers/**
      - src/etl/regions/**
      - test/etl/regions/**
      - lib/python/**
      - .github/workflows/etl_regions.yml
  workflow_dispatch:
    inputs:
      all:
        default: false
        description: Load the regions, changed or not.
        required: false
        type: boolean
permissions:
  contents: read
