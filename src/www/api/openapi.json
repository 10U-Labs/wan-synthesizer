{
  "openapi": "3.0.1",
  "info": {
    "title": "wan-synthesizer",
    "description": "Graphs as REST resources: carriers, providers, the carrier merge, and tenants' WANs. Inputs are writable; computed graphs are read-only. One JSON format throughout.",
    "version": "1.0.0"
  },
  "paths": {
    "/wan-synthesizer/carriers": {
      "get": {
        "summary": "List carriers.",
        "responses": { "200": { "description": "The carriers." } },
        "x-amazon-apigateway-integration": {
          "uri": "${CarriersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-synthesizer/carriers/{carrier}/pops": {
      "get": {
        "summary": "A carrier's PoPs.",
        "parameters": [
          { "name": "carrier", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The carrier's sites." } },
        "x-amazon-apigateway-integration": {
          "uri": "${CarriersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "put": {
        "summary": "Replace a carrier's PoPs (stores only).",
        "parameters": [
          { "name": "carrier", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The carrier's sites were replaced." } },
        "x-amazon-apigateway-integration": {
          "uri": "${CarriersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-synthesizer/carriers/{carrier}/fiber-segments": {
      "get": {
        "summary": "A carrier's fiber.",
        "parameters": [
          { "name": "carrier", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The carrier's links." } },
        "x-amazon-apigateway-integration": {
          "uri": "${CarriersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "put": {
        "summary": "Replace a carrier's fiber (stores only).",
        "parameters": [
          { "name": "carrier", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The carrier's links were replaced." } },
        "x-amazon-apigateway-integration": {
          "uri": "${CarriersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-synthesizer/carriers/{carrier}": {
      "delete": {
        "summary": "Remove a carrier.",
        "parameters": [
          { "name": "carrier", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The carrier was removed." } },
        "x-amazon-apigateway-integration": {
          "uri": "${CarriersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-synthesizer/store/prune": {
      "get": {
        "summary": "The objects stored under a name the product no longer writes.",
        "responses": { "200": { "description": "The stale keys, deleting nothing." } },
        "x-amazon-apigateway-integration": {
          "uri": "${PruneHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "post": {
        "summary": "Delete every object stored under a name the product no longer writes.",
        "requestBody": {
          "description": "The keys the caller wrote this run, which are kept whatever this deployment writes, so a seed never loses to the deploy of the name it just wrote.",
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "written": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    }
                  }
                }
              }
            }
          }
        },
        "responses": { "200": { "description": "The keys that went." } },
        "x-amazon-apigateway-integration": {
          "uri": "${PruneHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-synthesizer/carriers/merge": {
      "post": {
        "summary": "Merge all carriers onto one map (fast).",
        "responses": { "200": { "description": "The merged carriers." } },
        "x-amazon-apigateway-integration": {
          "uri": "${MergeHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "options": {
        "summary": "CORS preflight.",
        "security": [],
        "responses": {
          "200": {
            "description": "CORS.",
            "headers": {
              "Access-Control-Allow-Origin": { "schema": { "type": "string" } },
              "Access-Control-Allow-Methods": { "schema": { "type": "string" } },
              "Access-Control-Allow-Headers": { "schema": { "type": "string" } }
            }
          }
        },
        "x-amazon-apigateway-integration": {
          "type": "mock",
          "requestTemplates": { "application/json": "{\"statusCode\": 200}" },
          "responses": {
            "default": {
              "statusCode": "200",
              "responseParameters": {
                "method.response.header.Access-Control-Allow-Origin": "'https://www.10ulabs.com'",
                "method.response.header.Access-Control-Allow-Methods": "'GET,POST,OPTIONS'",
                "method.response.header.Access-Control-Allow-Headers": "'Authorization,Content-Type'"
              }
            }
          }
        }
      }
    },
    "/wan-synthesizer/carriers/merge/pops": {
      "get": {
        "summary": "The merged carriers' PoPs.",
        "responses": { "200": { "description": "Merged carrier sites." } },
        "x-amazon-apigateway-integration": {
          "uri": "${MergeHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-synthesizer/carriers/merge/fiber-segments": {
      "get": {
        "summary": "The merged carriers' fiber segments.",
        "responses": { "200": { "description": "Merged carrier links." } },
        "x-amazon-apigateway-integration": {
          "uri": "${MergeHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-synthesizer/providers/regions": {
      "get": {
        "summary": "The provider regions.",
        "responses": { "200": { "description": "The provider regions." } },
        "x-amazon-apigateway-integration": {
          "uri": "${ProvidersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "put": {
        "summary": "Replace the provider regions (stores only).",
        "responses": { "200": { "description": "The provider regions were replaced." } },
        "x-amazon-apigateway-integration": {
          "uri": "${ProvidersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "delete": {
        "summary": "Remove the provider regions.",
        "responses": { "200": { "description": "The provider regions were removed." } },
        "x-amazon-apigateway-integration": {
          "uri": "${ProvidersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-synthesizer/tenants": {
      "get": {
        "summary": "List tenants.",
        "responses": { "200": { "description": "The tenants, each as an {id, label} entry." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "options": {
        "summary": "CORS preflight.",
        "security": [],
        "responses": {
          "200": {
            "description": "CORS.",
            "headers": {
              "Access-Control-Allow-Origin": { "schema": { "type": "string" } },
              "Access-Control-Allow-Methods": { "schema": { "type": "string" } },
              "Access-Control-Allow-Headers": { "schema": { "type": "string" } }
            }
          }
        },
        "x-amazon-apigateway-integration": {
          "type": "mock",
          "requestTemplates": { "application/json": "{\"statusCode\": 200}" },
          "responses": {
            "default": {
              "statusCode": "200",
              "responseParameters": {
                "method.response.header.Access-Control-Allow-Origin": "'https://www.10ulabs.com'",
                "method.response.header.Access-Control-Allow-Methods": "'GET,OPTIONS'",
                "method.response.header.Access-Control-Allow-Headers": "'Authorization,Content-Type'"
              }
            }
          }
        }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/sites": {
      "get": {
        "summary": "A tenant WAN's sites.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The WAN's sites." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "options": {
        "summary": "CORS preflight.",
        "security": [],
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "CORS.",
            "headers": {
              "Access-Control-Allow-Origin": { "schema": { "type": "string" } },
              "Access-Control-Allow-Methods": { "schema": { "type": "string" } },
              "Access-Control-Allow-Headers": { "schema": { "type": "string" } }
            }
          }
        },
        "x-amazon-apigateway-integration": {
          "type": "mock",
          "requestTemplates": { "application/json": "{\"statusCode\": 200}" },
          "responses": {
            "default": {
              "statusCode": "200",
              "responseParameters": {
                "method.response.header.Access-Control-Allow-Origin": "'https://www.10ulabs.com'",
                "method.response.header.Access-Control-Allow-Methods": "'GET,OPTIONS'",
                "method.response.header.Access-Control-Allow-Headers": "'Authorization,Content-Type'"
              }
            }
          }
        }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/homing-circuits": {
      "get": {
        "summary": "A tenant WAN's circuits homing a demand site into a WAN PoP.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The homing circuits." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "options": {
        "summary": "CORS preflight.",
        "security": [],
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "CORS.",
            "headers": {
              "Access-Control-Allow-Origin": { "schema": { "type": "string" } },
              "Access-Control-Allow-Methods": { "schema": { "type": "string" } },
              "Access-Control-Allow-Headers": { "schema": { "type": "string" } }
            }
          }
        },
        "x-amazon-apigateway-integration": {
          "type": "mock",
          "requestTemplates": { "application/json": "{\"statusCode\": 200}" },
          "responses": {
            "default": {
              "statusCode": "200",
              "responseParameters": {
                "method.response.header.Access-Control-Allow-Origin": "'https://www.10ulabs.com'",
                "method.response.header.Access-Control-Allow-Methods": "'GET,OPTIONS'",
                "method.response.header.Access-Control-Allow-Headers": "'Authorization,Content-Type'"
              }
            }
          }
        }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/fiber-segments": {
      "get": {
        "summary": "The carrier fiber segments a tenant WAN's circuits run over.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The fiber segments." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "options": {
        "summary": "CORS preflight.",
        "security": [],
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "CORS.",
            "headers": {
              "Access-Control-Allow-Origin": { "schema": { "type": "string" } },
              "Access-Control-Allow-Methods": { "schema": { "type": "string" } },
              "Access-Control-Allow-Headers": { "schema": { "type": "string" } }
            }
          }
        },
        "x-amazon-apigateway-integration": {
          "type": "mock",
          "requestTemplates": { "application/json": "{\"statusCode\": 200}" },
          "responses": {
            "default": {
              "statusCode": "200",
              "responseParameters": {
                "method.response.header.Access-Control-Allow-Origin": "'https://www.10ulabs.com'",
                "method.response.header.Access-Control-Allow-Methods": "'GET,OPTIONS'",
                "method.response.header.Access-Control-Allow-Headers": "'Authorization,Content-Type'"
              }
            }
          }
        }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/wan-pops": {
      "get": {
        "summary": "A tenant WAN's selected WAN PoPs.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The WAN PoPs." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/backbone-circuits": {
      "get": {
        "summary": "A tenant WAN's circuits between two of its own WAN PoPs.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The backbone circuits." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "options": {
        "summary": "CORS preflight.",
        "security": [],
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "CORS.",
            "headers": {
              "Access-Control-Allow-Origin": { "schema": { "type": "string" } },
              "Access-Control-Allow-Methods": { "schema": { "type": "string" } },
              "Access-Control-Allow-Headers": { "schema": { "type": "string" } }
            }
          }
        },
        "x-amazon-apigateway-integration": {
          "type": "mock",
          "requestTemplates": { "application/json": "{\"statusCode\": 200}" },
          "responses": {
            "default": {
              "statusCode": "200",
              "responseParameters": {
                "method.response.header.Access-Control-Allow-Origin": "'https://www.10ulabs.com'",
                "method.response.header.Access-Control-Allow-Methods": "'GET,OPTIONS'",
                "method.response.header.Access-Control-Allow-Headers": "'Authorization,Content-Type'"
              }
            }
          }
        }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/tenant-sites": {
      "get": {
        "summary": "A tenant WAN's tenant-site demand, homed to the backbone.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The tenant's own sites." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/provider-sites": {
      "get": {
        "summary": "A tenant WAN's provider-region demand, homed to the backbone.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The provider regions the WAN reaches." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/locations": {
      "get": {
        "summary": "A tenant's demand locations (input).",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The locations." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "put": {
        "summary": "Replace a tenant's demand locations (stores only).",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The locations were replaced." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/provider-regions": {
      "get": {
        "summary": "A tenant's selected provider regions (input).",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The provider regions." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "put": {
        "summary": "Replace a tenant's provider regions (stores only).",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The provider regions were replaced." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/off-net": {
      "get": {
        "summary": "A tenant's off-net candidate sites (input).",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The off-net sites." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "put": {
        "summary": "Replace a tenant's off-net sites (stores only).",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The off-net sites were replaced." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/forced-wan-pops": {
      "get": {
        "summary": "A tenant's forced WAN PoPs (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The forced WAN PoPs." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's forced WAN PoPs (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The forced WAN PoPs were replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/forced-circuits": {
      "get": {
        "summary": "A tenant's forced circuits (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The forced circuits." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's forced circuits (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The forced circuits were replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/forced-homes": {
      "get": {
        "summary": "A tenant's forced homes (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The forced homes." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's forced homes (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The forced homes were replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/prohibited-wan-pops": {
      "get": {
        "summary": "A tenant's prohibited WAN PoPs (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The prohibited WAN PoPs." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's prohibited WAN PoPs (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The prohibited WAN PoPs were replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/degree-exempt-wan-pops": {
      "get": {
        "summary": "A tenant's degree-exempt WAN PoPs (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The degree-exempt WAN PoPs." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's degree-exempt WAN PoPs (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The degree-exempt WAN PoPs were replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/prohibited-circuits": {
      "get": {
        "summary": "A tenant's prohibited circuits (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The prohibited circuits." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's prohibited circuits (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The prohibited circuits were replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/wan-pop-count": {
      "get": {
        "summary": "A tenant's WAN PoP count, min and max (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The WAN PoP count." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's WAN PoP count (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The WAN PoP count was replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/convergence-promotion": {
      "get": {
        "summary": "A tenant's convergence promotion policy (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The convergence promotion policy." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's convergence promotion policy (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The convergence promotion policy was replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/backbone-number-of-diverse-circuits": {
      "get": {
        "summary": "A tenant's number of diverse circuits asked of every WAN PoP (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The number of diverse circuits." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's number of diverse circuits (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The number of diverse circuits was replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/homing-degree": {
      "get": {
        "summary": "A tenant's homing degree (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The homing degree." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's homing degree (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The homing degree was replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/knobs": {
      "get": {
        "summary": "A tenant's algorithm knobs (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The knobs." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's algorithm knobs (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The knobs were replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/settings": {
      "get": {
        "summary": "A tenant's implementation settings (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The settings." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's implementation settings (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The settings were replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/label": {
      "get": {
        "summary": "A tenant's display label (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The label." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's display label (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The label was replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-synthesizer/tenants/{tenant}": {
      "delete": {
        "summary": "Remove a tenant and all its inputs and outputs.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The tenant was removed." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-synthesizer/tenants/{tenant}/wan": {
      "post": {
        "summary": "Create (synthesize) the tenant's WAN.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "202": { "description": "Create started." },
          "422": { "description": "No valid WAN is possible." }
        },
        "x-amazon-apigateway-integration": {
          "uri": "${WanHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "get": {
        "summary": "The tenant's WAN and its status.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "The WAN: the state of the synthesis, which is a success only for a WAN the loss of no one PoP splits, the coverage it delivered, what each site was asked for, the fiber miles it runs over, and the fewest fiber miles the same requirements could have been met with.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "status": { "type": "string" },
                    "fiber_miles": {
                      "type": "number",
                      "description": "The carrier fiber miles this WAN's backbone circuits run over, the figure the floor beside it is stated under."
                    },
                    "backbone_lower_bound_miles": {
                      "type": "number",
                      "description": "The fewest fiber miles any backbone meeting this tenant's requirements over this carrier fiber could have run. Published so a reader outside the synthesizer can say how close the network they were handed is to the shortest one there is."
                    },
                    "homing_miles": {
                      "type": "object",
                      "description": "The miles this WAN's homing circuits run, apart: tenant for the tenant's own sites, provider for the cloud provider regions it uses. The two are published separately because a site and a region are not reached for the same reasons, not asked for by the same people, and not given up for the same reasons, so one figure covering both answers neither question.",
                      "properties": {
                        "tenant": { "type": "number" },
                        "provider": { "type": "number" }
                      }
                    }
                  }
                }
              }
            }
          }
        },
        "x-amazon-apigateway-integration": {
          "uri": "${WanHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "options": {
        "summary": "CORS preflight.",
        "security": [],
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "CORS.",
            "headers": {
              "Access-Control-Allow-Origin": { "schema": { "type": "string" } },
              "Access-Control-Allow-Methods": { "schema": { "type": "string" } },
              "Access-Control-Allow-Headers": { "schema": { "type": "string" } }
            }
          }
        },
        "x-amazon-apigateway-integration": {
          "type": "mock",
          "requestTemplates": { "application/json": "{\"statusCode\": 200}" },
          "responses": {
            "default": {
              "statusCode": "200",
              "responseParameters": {
                "method.response.header.Access-Control-Allow-Origin": "'https://www.10ulabs.com'",
                "method.response.header.Access-Control-Allow-Methods": "'GET,POST,OPTIONS'",
                "method.response.header.Access-Control-Allow-Headers": "'Authorization,Content-Type'"
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "bearer": {
        "type": "apiKey",
        "name": "Authorization",
        "in": "header",
        "description": "A Google ID token for an authorized account on the hosted domain, or the API key the seed holds, as a bearer token.",
        "x-amazon-apigateway-authtype": "custom",
        "x-amazon-apigateway-authorizer": {
          "type": "token",
          "authorizerUri": "${AuthorizerHandlerArn}",
          "identityValidationExpression": "^Bearer .+$",
          "authorizerResultTtlInSeconds": 300
        }
      }
    }
  },
  "security": [{ "bearer": [] }],
  "x-amazon-apigateway-gateway-responses": {
    "DEFAULT_4XX": {
      "responseParameters": { "gatewayresponse.header.Access-Control-Allow-Origin": "'https://www.10ulabs.com'" }
    },
    "DEFAULT_5XX": {
      "responseParameters": { "gatewayresponse.header.Access-Control-Allow-Origin": "'https://www.10ulabs.com'" }
    }
  }
}
