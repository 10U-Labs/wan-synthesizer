{
  "openapi": "3.0.1",
  "info": {
    "title": "wan-graph-synthesizer",
    "description": "Graphs as REST resources: carriers, providers, the carrier merge (substrate), and tenants' WANs. Inputs are writable; computed graphs are read-only. One JSON format throughout.",
    "version": "1.0.0"
  },
  "paths": {
    "/wan-graph-synthesizer/carriers": {
      "get": {
        "summary": "List carriers.",
        "responses": { "200": { "description": "The carriers." } },
        "x-amazon-apigateway-integration": {
          "uri": "${CarriersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/carriers/{carrier}/vertices": {
      "get": {
        "summary": "A carrier's PoPs.",
        "parameters": [
          { "name": "carrier", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The carrier's vertices." } },
        "x-amazon-apigateway-integration": {
          "uri": "${CarriersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "put": {
        "summary": "Replace a carrier's PoPs (stores only).",
        "parameters": [
          { "name": "carrier", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The carrier's vertices were replaced." } },
        "x-amazon-apigateway-integration": {
          "uri": "${CarriersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/carriers/{carrier}/edges": {
      "get": {
        "summary": "A carrier's fiber.",
        "parameters": [
          { "name": "carrier", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The carrier's edges." } },
        "x-amazon-apigateway-integration": {
          "uri": "${CarriersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "put": {
        "summary": "Replace a carrier's fiber (stores only).",
        "parameters": [
          { "name": "carrier", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The carrier's edges were replaced." } },
        "x-amazon-apigateway-integration": {
          "uri": "${CarriersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/carriers/{carrier}": {
      "delete": {
        "summary": "Remove a carrier.",
        "parameters": [
          { "name": "carrier", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The carrier was removed." } },
        "x-amazon-apigateway-integration": {
          "uri": "${CarriersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/carriers/merge": {
      "post": {
        "summary": "Merge all carriers into the substrate (fast).",
        "responses": { "200": { "description": "The substrate." } },
        "x-amazon-apigateway-integration": {
          "uri": "${MergeHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "options": {
        "summary": "CORS preflight.",
        "responses": {
          "200": {
            "description": "CORS.",
            "headers": {
              "Access-Control-Allow-Origin": { "schema": { "type": "string" } },
              "Access-Control-Allow-Methods": { "schema": { "type": "string" } },
              "Access-Control-Allow-Headers": { "schema": { "type": "string" } }
            }
          }
        },
        "x-amazon-apigateway-integration": {
          "type": "mock",
          "requestTemplates": { "application/json": "{\"statusCode\": 200}" },
          "responses": {
            "default": {
              "statusCode": "200",
              "responseParameters": {
                "method.response.header.Access-Control-Allow-Origin": "'*'",
                "method.response.header.Access-Control-Allow-Methods": "'GET,POST,OPTIONS'",
                "method.response.header.Access-Control-Allow-Headers": "'Content-Type'"
              }
            }
          }
        }
      }
    },
    "/wan-graph-synthesizer/carriers/merge/vertices": {
      "get": {
        "summary": "The substrate's vertices.",
        "responses": { "200": { "description": "Substrate vertices." } },
        "x-amazon-apigateway-integration": {
          "uri": "${MergeHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/carriers/merge/edges": {
      "get": {
        "summary": "The substrate's edges.",
        "responses": { "200": { "description": "Substrate edges." } },
        "x-amazon-apigateway-integration": {
          "uri": "${MergeHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/providers/vertices": {
      "get": {
        "summary": "The provider regions.",
        "responses": { "200": { "description": "The provider regions." } },
        "x-amazon-apigateway-integration": {
          "uri": "${ProvidersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "put": {
        "summary": "Replace the provider regions (stores only).",
        "responses": { "200": { "description": "The provider regions were replaced." } },
        "x-amazon-apigateway-integration": {
          "uri": "${ProvidersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "delete": {
        "summary": "Remove the provider regions.",
        "responses": { "200": { "description": "The provider regions were removed." } },
        "x-amazon-apigateway-integration": {
          "uri": "${ProvidersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/data-centers": {
      "get": {
        "summary": "List data-center providers.",
        "responses": { "200": { "description": "The data-center providers." } },
        "x-amazon-apigateway-integration": {
          "uri": "${DataCentersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/data-centers/{provider}/vertices": {
      "get": {
        "summary": "A provider's data-center facilities.",
        "parameters": [
          { "name": "provider", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The provider's facilities." } },
        "x-amazon-apigateway-integration": {
          "uri": "${DataCentersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "put": {
        "summary": "Replace a provider's facilities (stores only).",
        "parameters": [
          { "name": "provider", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The provider's facilities were replaced." } },
        "x-amazon-apigateway-integration": {
          "uri": "${DataCentersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/data-centers/{provider}": {
      "delete": {
        "summary": "Remove a data-center provider.",
        "parameters": [
          { "name": "provider", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The provider was removed." } },
        "x-amazon-apigateway-integration": {
          "uri": "${DataCentersHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/data-centers/merge": {
      "get": {
        "summary": "The data-center union's facilities.",
        "responses": { "200": { "description": "Union facilities." } },
        "x-amazon-apigateway-integration": {
          "uri": "${DataCentersMergeHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "post": {
        "summary": "Union all providers' facilities (fast).",
        "responses": { "200": { "description": "The data-center union." } },
        "x-amazon-apigateway-integration": {
          "uri": "${DataCentersMergeHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "options": {
        "summary": "CORS preflight.",
        "responses": {
          "200": {
            "description": "CORS.",
            "headers": {
              "Access-Control-Allow-Origin": { "schema": { "type": "string" } },
              "Access-Control-Allow-Methods": { "schema": { "type": "string" } },
              "Access-Control-Allow-Headers": { "schema": { "type": "string" } }
            }
          }
        },
        "x-amazon-apigateway-integration": {
          "type": "mock",
          "requestTemplates": { "application/json": "{\"statusCode\": 200}" },
          "responses": {
            "default": {
              "statusCode": "200",
              "responseParameters": {
                "method.response.header.Access-Control-Allow-Origin": "'*'",
                "method.response.header.Access-Control-Allow-Methods": "'GET,POST,OPTIONS'",
                "method.response.header.Access-Control-Allow-Headers": "'Content-Type'"
              }
            }
          }
        }
      }
    },
    "/wan-graph-synthesizer/tenants": {
      "get": {
        "summary": "List tenants.",
        "responses": { "200": { "description": "The tenants, each as an {id, label} entry." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/vertices": {
      "get": {
        "summary": "A tenant WAN's vertices.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The WAN's vertices." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/edges": {
      "get": {
        "summary": "A tenant WAN's edges.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The WAN's edges." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/backbone-nodes": {
      "get": {
        "summary": "A tenant WAN's backbone tier.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The backbone nodes." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/backbone-links": {
      "get": {
        "summary": "A tenant WAN's backbone-to-backbone mesh links.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The backbone links." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/tenant-nodes": {
      "get": {
        "summary": "A tenant WAN's tenant-site demand, homed to the backbone.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The tenant nodes." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/provider-nodes": {
      "get": {
        "summary": "A tenant WAN's provider-region demand, homed to the backbone.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The provider nodes." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/locations": {
      "get": {
        "summary": "A tenant's demand locations (input).",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The locations." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "put": {
        "summary": "Replace a tenant's demand locations (stores only).",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The locations were replaced." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/provider-regions": {
      "get": {
        "summary": "A tenant's selected provider regions (input).",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The provider regions." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "put": {
        "summary": "Replace a tenant's provider regions (stores only).",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The provider regions were replaced." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/off-net": {
      "get": {
        "summary": "A tenant's off-net candidate seats (input).",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The off-net seats." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "put": {
        "summary": "Replace a tenant's off-net seats (stores only).",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The off-net seats were replaced." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/forced-backbone-nodes": {
      "get": {
        "summary": "A tenant's forced backbone nodes (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The forced backbone nodes." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's forced backbone nodes (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The forced backbone nodes were replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/forced-paths": {
      "get": {
        "summary": "A tenant's forced paths (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The forced paths." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's forced paths (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The forced paths were replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/forced-homes": {
      "get": {
        "summary": "A tenant's forced homes (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The forced homes." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's forced homes (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The forced homes were replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/prohibited-backbone-nodes": {
      "get": {
        "summary": "A tenant's prohibited backbone nodes (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The prohibited backbone nodes." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's prohibited backbone nodes (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The prohibited backbone nodes were replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/degree-exempt-backbone-nodes": {
      "get": {
        "summary": "A tenant's degree-exempt backbone nodes (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The degree-exempt backbone nodes." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's degree-exempt backbone nodes (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The degree-exempt backbone nodes were replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/prohibited-paths": {
      "get": {
        "summary": "A tenant's prohibited paths (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The prohibited paths." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's prohibited paths (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The prohibited paths were replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/backbone-node-count": {
      "get": {
        "summary": "A tenant's backbone node count, min and max (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The backbone node count." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's backbone node count (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The backbone node count was replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/backbone-placement": {
      "get": {
        "summary": "A tenant's backbone placement policy (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The backbone placement policy." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's backbone placement policy (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The backbone placement policy was replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/convergence-promotion": {
      "get": {
        "summary": "A tenant's convergence promotion policy (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The convergence promotion policy." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's convergence promotion policy (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The convergence promotion policy was replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/backbone-number-of-diverse-paths": {
      "get": {
        "summary": "A tenant's backbone number of diverse paths (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The backbone number of diverse paths." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's backbone number of diverse paths (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The backbone number of diverse paths was replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/access-homing-degree": {
      "get": {
        "summary": "A tenant's access homing degree (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The access homing degree." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's access homing degree (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The access homing degree was replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/knobs": {
      "get": {
        "summary": "A tenant's algorithm knobs (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The knobs." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's algorithm knobs (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The knobs were replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/settings": {
      "get": {
        "summary": "A tenant's implementation settings (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The settings." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's implementation settings (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The settings were replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/label": {
      "get": {
        "summary": "A tenant's display label (input).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The label." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      },
      "put": {
        "summary": "Replace a tenant's display label (stores only).",
        "parameters": [ { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } } ],
        "responses": { "200": { "description": "The label was replaced." } },
        "x-amazon-apigateway-integration": { "uri": "${TenantsHandlerArn}", "httpMethod": "POST", "type": "aws_proxy" }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}": {
      "delete": {
        "summary": "Remove a tenant and all its inputs and outputs.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": { "200": { "description": "The tenant was removed." } },
        "x-amazon-apigateway-integration": {
          "uri": "${TenantsHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      }
    },
    "/wan-graph-synthesizer/tenants/{tenant}/wan": {
      "post": {
        "summary": "Create (synthesize) the tenant's WAN.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "202": { "description": "Create started." },
          "422": { "description": "No valid WAN is possible." }
        },
        "x-amazon-apigateway-integration": {
          "uri": "${WanHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "get": {
        "summary": "The tenant's WAN and its status.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "The WAN: the state of the build, the coverage it delivered, the backup path multiple it was built under, what each site was asked for, and the fewest fiber miles the same requirements could have been met with.",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "status": { "type": "string" },
                    "backbone_lower_bound_miles": {
                      "type": "number",
                      "description": "The fewest fiber miles any backbone meeting this tenant's requirements over this carrier fiber could have run. Published so a reader outside the build can say how close the network they were handed is to the shortest one there is."
                    }
                  }
                }
              }
            }
          }
        },
        "x-amazon-apigateway-integration": {
          "uri": "${WanHandlerArn}",
          "httpMethod": "POST",
          "type": "aws_proxy"
        }
      },
      "options": {
        "summary": "CORS preflight.",
        "parameters": [
          { "name": "tenant", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": {
            "description": "CORS.",
            "headers": {
              "Access-Control-Allow-Origin": { "schema": { "type": "string" } },
              "Access-Control-Allow-Methods": { "schema": { "type": "string" } },
              "Access-Control-Allow-Headers": { "schema": { "type": "string" } }
            }
          }
        },
        "x-amazon-apigateway-integration": {
          "type": "mock",
          "requestTemplates": { "application/json": "{\"statusCode\": 200}" },
          "responses": {
            "default": {
              "statusCode": "200",
              "responseParameters": {
                "method.response.header.Access-Control-Allow-Origin": "'*'",
                "method.response.header.Access-Control-Allow-Methods": "'GET,POST,OPTIONS'",
                "method.response.header.Access-Control-Allow-Headers": "'Content-Type'"
              }
            }
          }
        }
      }
    }
  }
}
